services:
  google_tag.response_subscriver:
    class: Drupal\google_tag\EventSubscriber\GoogleTagResponseSubscriber
    arguments: ['@config.factory', '@path.alias_manager', '@path.matcher', '@path.current']
    tags:
      - { name: event_subscriber }
